const config = {
  datoCms: {
    apiKey: process.env.DATOCMS_API_TOKEN,
    preview: 'https://graphql.datocms.com/preview',
    default: 'https://graphql.datocms.com/',
  },
};

export default config;
